[package]
name = "nue_storage"
version = "0.1.0"
edition = "2024"

[dependencies]
nue_model = { path = "../model" }
rusqlite = { version = "0.39.0", optional = true, features = ["bundled", "blob"] }
heed = "*"

[features]
default = []
alloc = []
sqlite = ["rusqlite"]
